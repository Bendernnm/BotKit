'use strict';

const GoodSchema = require('./good');
const UserSchema = require('./user');
const OrderSchema = require('./order');
const MessageSchema = require('./message');
const ReferralLinkSchema = require('./referral-link');

module.exports = {
    GoodSchema,
    UserSchema,
    OrderSchema,
    MessageSchema,
    ReferralLinkSchema
};
