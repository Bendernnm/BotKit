'use strict';

const UserModel = require('./user');
const GoodModel = require('./good');
const OrderModel = require('./order');
const MessageModel = require('./message');
const ReferralLinkModel = require('./referral-link');

module.exports = {
    UserModel,
    GoodModel,
    OrderModel,
    MessageModel,
    ReferralLinkModel
};
